# Obsidian Gate

## Publish to npm

```bash
npm publish
```

## Build

```bash
npm run build
```

## Test

```bash
npm test
```

## Before Publishing

1. Bump version in `package.json` if needed
2. Run `npm run build` — must compile clean
3. Run `npm publish` — need npm auth